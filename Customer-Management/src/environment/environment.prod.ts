// Production environment configuration.
// Replace api_url with the actual deployed backend URL before running `ng build`.
export const environment = {
    api_url: 'https://api.REPLACE_WITH_YOUR_DOMAIN.com',
};
